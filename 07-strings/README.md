# 07 - Strings

Working with text in Java. Strings are one of the most used types in any program.

Topics covered:
- Declaring and accessing strings
- String methods — length, charAt, substring, toUpperCase, contains, replace, trim
- String comparison — equals() vs ==
- String vs StringBuffer — mutable vs immutable
- StringBuilder for efficient string manipulation
