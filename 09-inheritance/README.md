# 09 - Inheritance

When one class inherits the properties and methods of another class using `extends`.

Topics covered:
- Single inheritance — one class extends another
- Multilevel inheritance — A extends B, B extends C
- Hierarchical inheritance — multiple classes extend the same parent
- Hybrid inheritance — combining multiple types
- Why multiple inheritance is not allowed directly in Java
